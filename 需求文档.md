# 🍒 Project Cherry - 开发文档

**版本:** v0.1.0-draft
**Slogan:** Cherry-pick the web.
**风格:** Retro Terminal / Pixel Art / Geek

## 1. 项目愿景 (Vision)

构建一个专属于程序员的浏览器起始页。不仅是链接的集合，更是一个微型的“Web 终端”。它应该具备 CRT 显示器的视觉复古感，同时拥有现代 Web 的响应速度，通过 Git 的工作流隐喻来管理日常访问的资源。

## 2. 技术栈选型 (Tech Stack)

考虑到你要“体现技术”且个人维护，推荐使用 **“现代静态站”** 架构，方便托管且加载极快。

- **核心框架:** **Vue 3** 或 **React** (配合 **Vite** 构建)。
- _理由:_ 组件化开发像素组件，生态丰富。

- **样式方案:** **Tailwind CSS** + 自定义 CSS (CRT 扫描线特效)。
- _推荐库:_ `NES.css` (8-bit 风格库) 或手撸简单的像素 CSS。

- **数据源:** **JSON / YAML** 文件。
- _理由:_ 不需要后端数据库。修改链接等于修改代码库中的 `.json` 文件，提交即发布，完美契合 Git 理念。

- **托管:** **GitHub Pages** 或 **Vercel**。
- _理由:_ 免费，自动化 CI/CD。

## 3. UI/UX 设计规范

### 3.1 视觉风格 (Visual Identity)

- **配色:**
- 背景: `#0d1117` (GitHub Dark Dimmed) 或 `#000000` (纯黑 CRT)。
- 主色: `#2ecc71` (Terminal Green) 或 `#ff0055` (Cherry Red - 用于高亮)。
- 文本: 等宽字体 (Monospace)，推荐 `Fira Code`, `VT323` (像素字体) 或 `JetBrains Mono`。

- **特效:**
- **CRT Scanline:** 屏幕微微的扫描线和辉光效果。
- **Typing Effect:** 页面加载时，Slogan 像在终端打字一样逐字出现。
- **Blink Cursor:** 永远闪烁的光标 `_`。

### 3.2 布局结构 (Layout)

1. **Header (Status Bar):**

- 左侧: `user@cherry:~$` (当前路径)
- 右侧: `branch: main` (显示当前分类) | `uptime: 100%`

2. **Hero Section:**

- 巨大的像素风 🍒 图标。
- 打字机动画显示 Slogan: `> Cherry-pick the web_`

3. **Command Input (Search Bar):**

- 伪装成命令行的搜索框。
- 输入 `g github` -> 跳转 Google 搜 GitHub。
- 输入 `pick <url>` -> (高级功能) 快速添加链接。

4. **Main Content (The Grid):**

- 分类不叫“分类”，叫 **Branches**。
- 链接卡片像一个个代码块 (Block)。

## 4. 数据结构设计 (Data Schema)

使用 `data.json` 存储所有配置，方便维护。

```json
{
  "site_config": {
    "user_name": "DevUser",
    "theme": "dark_matrix",
    "slogan": "Cherry-pick the web"
  },
  "branches": [  // 即分类
    {
      "name": "feature/work",
      "icon": "💼",
      "commits": [  // 即链接列表
        {
          "message": "GitHub Dashboard", // 链接标题
          "hash": "a1b2c3d", // 装饰用的伪 Hash
          "url": "https://github.com",
          "tags": ["code", "daily"]
        },
        {
          "message": "ChatGPT",
          "hash": "f4e5d6c",
          "url": "https://chatgpt.com",
          "tags": ["ai"]
        }
      ]
    },
    {
      "name": "hotfix/tools",
      "icon": "🛠️",
      "commits": [ ... ]
    }
  ]
}

```

## 5. 核心功能点 (Features)

### 5.1 键盘优先 (Vim Mode)

作为极客导航站，必须支持纯键盘操作：

- `j` / `k`: 上下切换链接卡片。
- `h` / `l`: 切换 Branch (分类)。
- `Enter`: 打开选中链接。
- `/`: 聚焦到搜索框 (Command Input)。
- `Esc`: 清除搜索/失焦。

### 5.2 命令行搜索 (Command Palette)

搜索框不仅支持搜索链接，还支持“伪命令”：

- `help`: 列出快捷键。
- `ls`: 列出所有分类。
- `go <index>`: 跳转到第 N 个链接。

### 5.3 响应式像素画

在宽屏下显示完整的像素 Cherry 树，在移动端简化为单个像素 Cherry。

## 6. 开发路线图 (Roadmap)

### Phase 1: Hello World (MVP)

- [ ] 搭建 Vite + React/Vue 环境。
- [ ] 引入像素字体 (Google Fonts VT323)。
- [ ] 实现 CRT 屏幕 CSS 特效。
- [ ] 解析 `data.json` 并渲染出静态列表。

### Phase 2: Interactive (交互增强)

- [ ] 实现 Vim 快捷键绑定 (使用 `react-use-keypress` 或原生监听)。
- [ ] 完成“命令行”搜索框样式与逻辑。
- [ ] 添加 hover 时的像素抖动/反色效果。

### Phase 3: Cherry-pick Logic (高级功能)

- [ ] 实现 `Add` 功能：虽然是静态站，可以做一个输入框，输入 URL 后生成对应的 JSON 片段复制到剪贴板，方便你粘贴到代码里（手动 Cherry-pick）。

## 7. 示例代码片段 (CSS CRT Effect)

给你一段 CSS 核心代码，瞬间提升逼格：

```css
/* CRT Screen Effect */
body {
  background-color: #111;
  color: #0f0; /* Terminal Green */
  font-family: "VT323", monospace;
  position: relative;
  overflow-x: hidden;
}

/* 扫描线 */
body::before {
  content: " ";
  display: block;
  position: absolute;
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
  background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%),
    linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06));
  z-index: 2;
  background-size: 100% 2px, 3px 100%;
  pointer-events: none;
}

/* 屏幕微微闪烁 */
@keyframes flicker {
  0% {
    opacity: 0.97;
  }
  5% {
    opacity: 0.95;
  }
  10% {
    opacity: 0.9;
  }
  ...;
}
```
